services:
  bot:
    build: .
    container_name: ai-marketer-bot
    restart: unless-stopped
    network_mode: host
    env_file:
      - .env
    volumes:
      - ./cardgen/data:/app/cardgen/data
      - ./cardgen:/app/cardgen
