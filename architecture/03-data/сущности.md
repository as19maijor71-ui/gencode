# Сущности

## Основные сущности (MVP)

### CardResult (Pydantic BaseModel)
Главная сущность. Результат генерации. Описана в [[главный-кирпич]].

### Category (Enum/Pydantic)
```python
class Category(str, Enum):
    GENERIC = "generic"
    CLOTHING = "одежда"
    FOOTWEAR = "обувь"
    ELECTRONICS = "электроника"
    COSMETICS = "косметика"
    HOME = "дом"
    CHILDREN = "детские_товары"
```

### User (неявная, FSM)
```python
# Состояния FSM
class CardState(StatesGroup):
    category_select = State()
    product_input = State()
    generating = State()
```

### AlgorithmRules (концептуальная)
Два модуля:
- `engine/wb_algorithm.py` → `get_wb_rules() -> str`
- `engine/ozon_algorithm.py` → `get_ozon_rules() -> str`

## Сущности будущих фаз

### Фаза 2: User (БД)
```python
class User(BaseModel):
    id: int
    telegram_id: int
    created_at: datetime
    generations_used: int
    plan: str  # "free" | "pro"
```

### Фаза 3: CardHistory
```python
class CardHistory(BaseModel):
    id: int
    user_id: int
    product_description: str
    result: CardResult
    created_at: datetime
```
